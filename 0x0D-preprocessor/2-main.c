#include <stdio.h>

/**
 * main - print the name of the file when complied
 * followed by a new line
 * Return: 0
 */

int main(void)
{
	printf("The name of the file = %s\n", __FILE__);
	return (0);
}
